forward B
type A { name: string, link: B }
type B { name: string, link: A }

scope {
    create A(
        tag(A),
        name="One",
        link=B
    )
    create B(
        tag(B),
        name="Two",
        link=A
    )
}
