-- TTQ dump
forward type A
type B { name: string, link: A }
type A { name: string, link: B }

scope {
    create B(
        tag(A),
        name="Two",
        link=A(
            name="One",
            link=A
        )
    )
}
